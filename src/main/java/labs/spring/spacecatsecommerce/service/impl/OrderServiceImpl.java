//package labs.spring.spacecatsecommerce.service.impl;
//
//import labs.spring.spacecatsecommerce.domain.Order;
//import labs.spring.spacecatsecommerce.dto.OrderDTO;
//import labs.spring.spacecatsecommerce.service.OrderService;
//import org.springframework.stereotype.Service;
//
//import java.util.ArrayList;
//import java.util.List;
//
//@Service
//public class OrderServiceImpl implements OrderService {
//
//  //  private final List<Order> orders = new ArrayList<>(); // In-memory storage for demonstration
//
//    @Override
//    public Order getOrderById(Long orderId) {
////        return orders.stream()
////                .filter(order -> order.getId().equals(orderId))
////                .findFirst()
////                .orElse(null); // Return null if not found
//        return null;
//    }
//
//    @Override
//    public Order createOrder(OrderDTO orderDTO) {
////        Order order = new Order(); // Create a new order from DTO
////        order.setId((long) (orders.size() + 1)); // Generate a simple ID
////        order.setProductId(orderDTO.getProductId());
////        order.setQuantity(orderDTO.getQuantity());
////        orders.add(order); // Add to the in-memory list
////        return order;
//        return null;
//    }
//}
